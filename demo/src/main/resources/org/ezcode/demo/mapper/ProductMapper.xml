<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ezcode.demo.mapper.ProductMapper">
<select id="selectAll" resultType="org.ezcode.demo.domain.ProductVO">
    select pno, ctno, seller, pname, price, summary, explanation, hit, regdate, state FROM tbl_product
</select>
<insert id="insert">
 insert into tbl_product (ctno, seller,pname, price, summary, explanation,  state)
  values(#{ctno},#{seller},#{pname},#{price},#{summary}, #{explanation}, '판매중')
</insert>
</mapper> 